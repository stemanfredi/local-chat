# Server configuration
PORT=3000
HOST=localhost

# JWT secret (generate a random string for production: openssl rand -base64 32)
JWT_SECRET=change-this-to-a-random-secret

# Database path
DB_PATH=./data/local-chat.db
